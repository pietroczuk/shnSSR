export type PublicConfig_DefaultVariantCode = {
    [key: string]: {
        code: string,
        atrib_id: string,
        wishlist: boolean
    }
}

export type PublicConfig_Translation = {
    [key: string]: string
}